{
  "name": "web3-script",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "cd frontend && next dev -- -p 3003",
    "build": "cd frontend && next build",
    "start": "cd frontend && next build && next start -- -p 3001"
  },
  "dependencies": {
    "@headlessui/react": "^2.2.0"
  }
}
